---
:backends:
  - yaml

:hierarchy:
  - "osfamily/%{::osfamily}"
  - "role/%{::role}"
  - "tier/%{::tier}"
  - common

:yaml:
  :datadir: "/etc/puppetlabs/puppet/environments/%{::environment}/hieradata"
